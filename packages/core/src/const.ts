export enum NodeType {
    CONTAINER = 'container',
    PAGE = 'page',
    ROOT = 'root',
}

export enum EventType {
    CLICK = 'click',
    CHANGE = 'change',
    TOUCH = 'touch',
}

export enum ActionType {
    COMP = 'comp',
    FUNC = 'func'
}