export enum NodeType {
    CONTAINER = 'container',
    PAGE = 'page',
    ROOT = 'root',
    NODE = 'node'
}

export enum EventType {
    CLICK = 'click',
    CHANGE = 'change',
    TOUCH = 'touch',
}

export enum ActionType {
    COMP = 'comp',
    /** 数据源 */
    DATA_SOURCE = 'data-source',
}
